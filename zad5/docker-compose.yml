services:
  server:
    build: ./server
    ports:
      - "3001:3001"

  client:
    build: ./client
    ports:
      - "5173:5173"
    environment:
      - VITE_API_URL=http://server:3001
    depends_on:
      - server